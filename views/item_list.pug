extends layout

block content
  h1= title

  ul
    - item_list.sort();
    each item in item_list
      li
        a(href=item.url) #{item.name}
        |  (#{item.category.name})

    else
      li There are no items.